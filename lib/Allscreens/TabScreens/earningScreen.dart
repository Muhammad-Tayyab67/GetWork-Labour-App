import 'package:flutter/cupertino.dart';

class earningScreen extends StatelessWidget {
  const earningScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is earning Screen"),
    );
  }
}

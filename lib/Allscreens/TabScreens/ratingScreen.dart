import 'package:flutter/cupertino.dart';

class rationScreen extends StatelessWidget {
  const rationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is rating screen"),
    );
  }
}
